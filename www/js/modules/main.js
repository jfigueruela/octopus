/**
 * Widgets module RequireJS configuration.
 * 
 * @param {type} angular
 * @author Jose Figueruela <jfigueruela@gmail.com>
 * @since
 */

(function() {
  define([
      // Here files modules internal files referencies.
      // Widgets module instance itself.
      './modules.module', './d3/main', './home/main'
  ], function() {
  });
})();
